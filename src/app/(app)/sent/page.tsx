import { EmptyState } from "@/components/ui/EmptyState";

export default function SentIndexPage() {
  return <EmptyState title="select a recommendation" description="pick something from the list to see what happened." />;
}
