import { EmptyState } from "@/components/ui/EmptyState";

export default function InboxIndexPage() {
  return <EmptyState title="select a recommendation" description="pick something from the list to see what it's about." />;
}
